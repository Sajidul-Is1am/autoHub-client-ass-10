import { useLoaderData } from "react-router-dom";

const MyCard = () => {
    const products = useLoaderData();
    return (
        <div>
            
        </div>
    );
};

export default MyCard;